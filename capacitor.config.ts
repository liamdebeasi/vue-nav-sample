import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vue-nav-example',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
